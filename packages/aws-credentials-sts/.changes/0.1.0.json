{
  "changes": [
    {
      "type": "feature",
      "description": "Add STS AssumeRole credential resolvers and `ProfileAssumeRole` chain provider."
    },
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-sts` from `~=0.8.0` to `~=0.9.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-aws-core` from `~=0.8.0` to `~=0.9.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-core` from `~=0.7.0` to `~=0.8.0`."
    }
  ]
}
