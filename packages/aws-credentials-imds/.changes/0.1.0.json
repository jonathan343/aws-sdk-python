{
  "changes": [
    {
      "type": "feature",
      "description": "Add EC2 Instance Metadata Service (IMDSv2) credentials resolver and `Ec2InstanceMetadata` chain provider."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-aws-core` from `~=0.8.0` to `~=0.9.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-core` from `~=0.7.0` to `~=0.8.0`."
    }
  ]
}
