{
  "changes": [
    {
      "type": "feature",
      "description": "Initial client release with support for current AWS Security Token Service operations."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-aws-core` from `~=0.7.0` to `~=0.8.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-core` from `~=0.6.0` to `~=0.7.0`."
    }
  ]
}
