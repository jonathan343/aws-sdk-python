# Code generated by smithy-python-codegen DO NOT EDIT.

from dataclasses import dataclass, field
from datetime import datetime, timezone
import logging
from typing import Any, Literal, Self

from smithy_core.deserializers import ShapeDeserializer
from smithy_core.documents import TypeRegistry
from smithy_core.exceptions import ModeledError
from smithy_core.schemas import APIOperation, Schema
from smithy_core.serializers import ShapeSerializer
from smithy_core.shapes import ShapeID

from ._private.schemas import (
    ASSUMED_ROLE_USER as _SCHEMA_ASSUMED_ROLE_USER,
    ASSUME_ROLE as _SCHEMA_ASSUME_ROLE,
    ASSUME_ROLE_INPUT as _SCHEMA_ASSUME_ROLE_INPUT,
    ASSUME_ROLE_OUTPUT as _SCHEMA_ASSUME_ROLE_OUTPUT,
    ASSUME_ROLE_WITH_SAML as _SCHEMA_ASSUME_ROLE_WITH_SAML,
    ASSUME_ROLE_WITH_SAML_INPUT as _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT,
    ASSUME_ROLE_WITH_SAML_OUTPUT as _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT,
    ASSUME_ROLE_WITH_WEB_IDENTITY as _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY,
    ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT as _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT,
    ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT as _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT,
    ASSUME_ROOT as _SCHEMA_ASSUME_ROOT,
    ASSUME_ROOT_INPUT as _SCHEMA_ASSUME_ROOT_INPUT,
    ASSUME_ROOT_OUTPUT as _SCHEMA_ASSUME_ROOT_OUTPUT,
    CREDENTIALS as _SCHEMA_CREDENTIALS,
    DECODE_AUTHORIZATION_MESSAGE as _SCHEMA_DECODE_AUTHORIZATION_MESSAGE,
    DECODE_AUTHORIZATION_MESSAGE_INPUT as _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_INPUT,
    DECODE_AUTHORIZATION_MESSAGE_OUTPUT as _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_OUTPUT,
    EXPIRED_TOKEN_EXCEPTION as _SCHEMA_EXPIRED_TOKEN_EXCEPTION,
    EXPIRED_TRADE_IN_TOKEN_EXCEPTION as _SCHEMA_EXPIRED_TRADE_IN_TOKEN_EXCEPTION,
    FEDERATED_USER as _SCHEMA_FEDERATED_USER,
    GET_ACCESS_KEY_INFO as _SCHEMA_GET_ACCESS_KEY_INFO,
    GET_ACCESS_KEY_INFO_INPUT as _SCHEMA_GET_ACCESS_KEY_INFO_INPUT,
    GET_ACCESS_KEY_INFO_OUTPUT as _SCHEMA_GET_ACCESS_KEY_INFO_OUTPUT,
    GET_CALLER_IDENTITY as _SCHEMA_GET_CALLER_IDENTITY,
    GET_CALLER_IDENTITY_INPUT as _SCHEMA_GET_CALLER_IDENTITY_INPUT,
    GET_CALLER_IDENTITY_OUTPUT as _SCHEMA_GET_CALLER_IDENTITY_OUTPUT,
    GET_DELEGATED_ACCESS_TOKEN as _SCHEMA_GET_DELEGATED_ACCESS_TOKEN,
    GET_DELEGATED_ACCESS_TOKEN_INPUT as _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_INPUT,
    GET_DELEGATED_ACCESS_TOKEN_OUTPUT as _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT,
    GET_FEDERATION_TOKEN as _SCHEMA_GET_FEDERATION_TOKEN,
    GET_FEDERATION_TOKEN_INPUT as _SCHEMA_GET_FEDERATION_TOKEN_INPUT,
    GET_FEDERATION_TOKEN_OUTPUT as _SCHEMA_GET_FEDERATION_TOKEN_OUTPUT,
    GET_SESSION_TOKEN as _SCHEMA_GET_SESSION_TOKEN,
    GET_SESSION_TOKEN_INPUT as _SCHEMA_GET_SESSION_TOKEN_INPUT,
    GET_SESSION_TOKEN_OUTPUT as _SCHEMA_GET_SESSION_TOKEN_OUTPUT,
    GET_WEB_IDENTITY_TOKEN as _SCHEMA_GET_WEB_IDENTITY_TOKEN,
    GET_WEB_IDENTITY_TOKEN_INPUT as _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT,
    GET_WEB_IDENTITY_TOKEN_OUTPUT as _SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT,
    IDP_COMMUNICATION_ERROR_EXCEPTION as _SCHEMA_IDP_COMMUNICATION_ERROR_EXCEPTION,
    IDP_REJECTED_CLAIM_EXCEPTION as _SCHEMA_IDP_REJECTED_CLAIM_EXCEPTION,
    INVALID_AUTHORIZATION_MESSAGE_EXCEPTION as _SCHEMA_INVALID_AUTHORIZATION_MESSAGE_EXCEPTION,
    INVALID_IDENTITY_TOKEN_EXCEPTION as _SCHEMA_INVALID_IDENTITY_TOKEN_EXCEPTION,
    JWT_PAYLOAD_SIZE_EXCEEDED_EXCEPTION as _SCHEMA_JWT_PAYLOAD_SIZE_EXCEEDED_EXCEPTION,
    MALFORMED_POLICY_DOCUMENT_EXCEPTION as _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION,
    OUTBOUND_WEB_IDENTITY_FEDERATION_DISABLED_EXCEPTION as _SCHEMA_OUTBOUND_WEB_IDENTITY_FEDERATION_DISABLED_EXCEPTION,
    PACKED_POLICY_TOO_LARGE_EXCEPTION as _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION,
    POLICY_DESCRIPTOR_TYPE as _SCHEMA_POLICY_DESCRIPTOR_TYPE,
    PROVIDED_CONTEXT as _SCHEMA_PROVIDED_CONTEXT,
    REGION_DISABLED_EXCEPTION as _SCHEMA_REGION_DISABLED_EXCEPTION,
    SESSION_DURATION_ESCALATION_EXCEPTION as _SCHEMA_SESSION_DURATION_ESCALATION_EXCEPTION,
    TAG as _SCHEMA_TAG,
)


logger = logging.getLogger(__name__)


class ServiceError(ModeledError):
    """
    Base error for all errors in the service.

    Some exceptions do not extend from this class, including
    synthetic, implicit, and shared exception types.
    """


@dataclass(kw_only=True)
class AssumedRoleUser:
    """
    The identifiers for the temporary security credentials that the
    operation returns.
    """

    assumed_role_id: str
    """
    A unique identifier that contains the role ID and the role session name
    of the role that is being assumed. The role ID is generated by Amazon
    Web Services when the role is created.
    """

    arn: str
    """
    The ARN of the temporary security credentials that are returned from the
    AssumeRole action. For more information about ARNs and how to use them
    in policies, see [IAM
    Identifiers](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html)
    in the *IAM User Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUMED_ROLE_USER, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_ASSUMED_ROLE_USER.members["AssumedRoleId"], self.assumed_role_id
        )
        serializer.write_string(_SCHEMA_ASSUMED_ROLE_USER.members["Arn"], self.arn)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["assumed_role_id"] = de.read_string(
                        _SCHEMA_ASSUMED_ROLE_USER.members["AssumedRoleId"]
                    )

                case 1:
                    kwargs["arn"] = de.read_string(
                        _SCHEMA_ASSUMED_ROLE_USER.members["Arn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ASSUMED_ROLE_USER, consumer=_consumer)
        if "assumed_role_id" not in kwargs:
            kwargs["assumed_role_id"] = ""
        if "arn" not in kwargs:
            kwargs["arn"] = ""
        return kwargs


@dataclass(kw_only=True)
class PolicyDescriptorType:
    """
    A reference to the IAM managed policy that is passed as a session policy
    for a role session or a federated user session.
    """

    arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the IAM managed policy to use as a
    session policy for the role. For more information about ARNs, see
    [Amazon Resource Names (ARNs) and Amazon Web Services Service
    Namespaces](https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
    in the *Amazon Web Services General Reference*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_POLICY_DESCRIPTOR_TYPE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.arn is not None:
            serializer.write_string(
                _SCHEMA_POLICY_DESCRIPTOR_TYPE.members["arn"], self.arn
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["arn"] = de.read_string(
                        _SCHEMA_POLICY_DESCRIPTOR_TYPE.members["arn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_POLICY_DESCRIPTOR_TYPE, consumer=_consumer)
        return kwargs


def _serialize_policy_descriptor_list_type(
    serializer: ShapeSerializer, schema: Schema, value: list[PolicyDescriptorType]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_policy_descriptor_list_type(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[PolicyDescriptorType]:
    result: list[PolicyDescriptorType] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(PolicyDescriptorType.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ProvidedContext:
    """
    Contains information about the provided context. This includes the
    signed and encrypted trusted context assertion and the context provider
    ARN from which the trusted context assertion was generated.
    """

    provider_arn: str | None = None
    """
    The context provider ARN from which the trusted context assertion was
    generated.
    """

    context_assertion: str | None = None
    """
    The signed and encrypted trusted context assertion generated by the
    context provider. The trusted context assertion is signed and encrypted
    by Amazon Web Services STS.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PROVIDED_CONTEXT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.provider_arn is not None:
            serializer.write_string(
                _SCHEMA_PROVIDED_CONTEXT.members["ProviderArn"], self.provider_arn
            )

        if self.context_assertion is not None:
            serializer.write_string(
                _SCHEMA_PROVIDED_CONTEXT.members["ContextAssertion"],
                self.context_assertion,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["provider_arn"] = de.read_string(
                        _SCHEMA_PROVIDED_CONTEXT.members["ProviderArn"]
                    )

                case 1:
                    kwargs["context_assertion"] = de.read_string(
                        _SCHEMA_PROVIDED_CONTEXT.members["ContextAssertion"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PROVIDED_CONTEXT, consumer=_consumer)
        return kwargs


def _serialize_provided_contexts_list_type(
    serializer: ShapeSerializer, schema: Schema, value: list[ProvidedContext]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_provided_contexts_list_type(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ProvidedContext]:
    result: list[ProvidedContext] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ProvidedContext.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class Tag:
    """
    You can pass custom key-value pair attributes when you assume a role or
    federate a user. These are called session tags. You can then use the
    session tags to control access to resources. For more information, see
    [Tagging Amazon Web Services STS
    Sessions](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_session-tags.html)
    in the *IAM User Guide*.
    """

    key: str
    """
    The key for a session tag.

    You can pass up to 50 session tags. The plain text session tag keys
    can't exceed 128 characters. For these and additional limits, see [IAM
    and STS Character
    Limits](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-limits.html#reference_iam-limits-entity-length)
    in the *IAM User Guide*.
    """

    value: str
    """
    The value for a session tag.

    You can pass up to 50 session tags. The plain text session tag values
    can't exceed 256 characters. For these and additional limits, see [IAM
    and STS Character
    Limits](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-limits.html#reference_iam-limits-entity-length)
    in the *IAM User Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TAG, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_TAG.members["Key"], self.key)
        serializer.write_string(_SCHEMA_TAG.members["Value"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["key"] = de.read_string(_SCHEMA_TAG.members["Key"])

                case 1:
                    kwargs["value"] = de.read_string(_SCHEMA_TAG.members["Value"])

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TAG, consumer=_consumer)
        if "key" not in kwargs:
            kwargs["key"] = ""
        if "value" not in kwargs:
            kwargs["value"] = ""
        return kwargs


def _serialize_tag_list_type(
    serializer: ShapeSerializer, schema: Schema, value: list[Tag]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_tag_list_type(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[Tag]:
    result: list[Tag] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Tag.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_tag_key_list_type(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_tag_key_list_type(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class AssumeRoleInput:
    """Dataclass for AssumeRoleInput structure."""

    role_arn: str | None = None
    """The Amazon Resource Name (ARN) of the role to assume."""

    role_session_name: str | None = None
    """
    An identifier for the assumed role session.

    Use the role session name to uniquely identify a session when the same
    role is assumed by different principals or for different reasons. In
    cross-account scenarios, the role session name is visible to, and can be
    logged by the account that owns the role. The role session name is also
    used in the ARN of the assumed role principal. This means that
    subsequent cross-account API requests that use the temporary security
    credentials will expose the role session name to the external account in
    their CloudTrail logs.

    For security purposes, administrators can view this field in [CloudTrail
    logs](https://docs.aws.amazon.com/IAM/latest/UserGuide/cloudtrail-integration.html#cloudtrail-integration_signin-tempcreds)
    to help identify who performed an action in Amazon Web Services. Your
    administrator might require that you specify your user name as the
    session name when you assume the role. For more information, see
    [`sts:RoleSessionName`](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_iam-condition-keys.html#ck_rolesessionname)
    .

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: +=,.@-
    """

    policy_arns: list[PolicyDescriptorType] | None = None
    """
    The Amazon Resource Names (ARNs) of the IAM managed policies that you
    want to use as managed session policies. The policies must exist in the
    same account as the role.

    This parameter is optional. You can provide up to 10 managed policy
    ARNs. However, the plaintext that you use for both inline and managed
    session policies can't exceed 2,048 characters. For more information
    about ARNs, see [Amazon Resource Names (ARNs) and Amazon Web Services
    Service
    Namespaces](https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
    in the Amazon Web Services General Reference.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.

    Passing policies to this operation returns new temporary credentials.
    The resulting session's permissions are the intersection of the role's
    identity-based policy and the session policies. You can use the role's
    temporary credentials in subsequent Amazon Web Services API calls to
    access resources in the account that owns the role. You cannot use
    session policies to grant more permissions than those allowed by the
    identity-based policy of the role that is being assumed. For more
    information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.
    """

    policy: str | None = None
    """
    An IAM policy in JSON format that you want to use as an inline session
    policy.

    This parameter is optional. Passing policies to this operation returns
    new temporary credentials. The resulting session's permissions are the
    intersection of the role's identity-based policy and the session
    policies. You can use the role's temporary credentials in subsequent
    Amazon Web Services API calls to access resources in the account that
    owns the role. You cannot use session policies to grant more permissions
    than those allowed by the identity-based policy of the role that is
    being assumed. For more information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.

    The plaintext that you use for both inline and managed session policies
    can't exceed 2,048 characters. The JSON policy characters can be any
    ASCII character from the space character to the end of the valid
    character list (\\u0020 through \\u00FF). It can also include the tab
    (\\u0009), linefeed (\\u000A), and carriage return (\\u000D) characters.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.

    For more information about role session permissions, see [Session
    policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session).
    """

    duration_seconds: int | None = None
    """
    The duration, in seconds, of the role session. The value specified can
    range from 900 seconds (15 minutes) up to the maximum session duration
    set for the role. The maximum session duration setting can have a value
    from 1 hour to 12 hours. If you specify a value higher than this setting
    or the administrator setting (whichever is lower), the operation fails.
    For example, if you specify a session duration of 12 hours, but your
    administrator set the maximum session duration to 6 hours, your
    operation fails.

    Role chaining limits your Amazon Web Services CLI or Amazon Web Services
    API role session to a maximum of one hour. When you use the `AssumeRole`
    API operation to assume a role, you can specify the duration of your
    role session with the `DurationSeconds` parameter. You can specify a
    parameter value of up to 43200 seconds (12 hours), depending on the
    maximum session duration setting for your role. However, if you assume a
    role using role chaining and provide a `DurationSeconds` parameter value
    greater than one hour, the operation fails. To learn how to view the
    maximum value for your role, see [Update the maximum session duration
    for a
    role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_update-role-settings.html#id_roles_update-session-duration).

    By default, the value is set to `3600` seconds.

    Note:
        The `DurationSeconds` parameter is separate from the duration of a
        console session that you might request using the returned credentials.
        The request to the federation endpoint for a console sign-in token takes
        a `SessionDuration` parameter that specifies the maximum length of the
        console session. For more information, see [Creating a URL that Enables
        Federated Users to Access the Amazon Web Services Management
        Console](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_enable-console-custom-url.html)
        in the *IAM User Guide*.
    """

    tags: list[Tag] | None = None
    """
    A list of session tags that you want to pass. Each session tag consists
    of a key name and an associated value. For more information about
    session tags, see [Tagging Amazon Web Services STS
    Sessions](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_session-tags.html)
    in the *IAM User Guide*.

    This parameter is optional. You can pass up to 50 session tags. The
    plaintext session tag keys can't exceed 128 characters, and the values
    can't exceed 256 characters. For these and additional limits, see [IAM
    and STS Character
    Limits](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-limits.html#reference_iam-limits-entity-length)
    in the *IAM User Guide*.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.

    You can pass a session tag with the same key as a tag that is already
    attached to the role. When you do, session tags override a role tag with
    the same key.

    Tag key--value pairs are not case sensitive, but case is preserved. This
    means that you cannot have separate `Department` and `department` tag
    keys. Assume that the role has the `Department`=`Marketing` tag and you
    pass the `department`=`engineering` session tag. `Department` and
    `department` are not saved as separate tags, and the session tag passed
    in the request takes precedence over the role tag.

    Additionally, if you used temporary credentials to perform this
    operation, the new session inherits any transitive session tags from the
    calling session. If you pass a session tag with the same key as an
    inherited tag, the operation fails. To view the inherited tags for a
    session, see the CloudTrail logs. For more information, see [Viewing
    Session Tags in
    CloudTrail](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_session-tags.html#id_session-tags_ctlogs)
    in the *IAM User Guide*.
    """

    transitive_tag_keys: list[str] | None = None
    """
    A list of keys for session tags that you want to set as transitive. If
    you set a tag key as transitive, the corresponding key and value passes
    to subsequent sessions in a role chain. For more information, see
    [Chaining Roles with Session
    Tags](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_session-tags.html#id_session-tags_role-chaining)
    in the *IAM User Guide*.

    This parameter is optional. The transitive status of a session tag does
    not impact its packed binary size.

    If you choose not to specify a transitive tag key, then no tags are
    passed from this session to any subsequent sessions.
    """

    external_id: str | None = None
    """
    A unique identifier that might be required when you assume a role in
    another account. If the administrator of the account to which the role
    belongs provided you with an external ID, then provide that value in the
    `ExternalId` parameter. This value can be any string, such as a
    passphrase or account number. A cross-account role is usually set up to
    trust everyone in an account. Therefore, the administrator of the
    trusting account might send an external ID to the administrator of the
    trusted account. That way, only someone with the ID can assume the role,
    rather than everyone in the account. For more information about the
    external ID, see [How to Use an External ID When Granting Access to Your
    Amazon Web Services Resources to a Third
    Party](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_create_for-user_externalid.html)
    in the *IAM User Guide*.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: +=,.@:\\/-
    """

    serial_number: str | None = None
    """
    The identification number of the MFA device that is associated with the
    user who is making the `AssumeRole` call. Specify this value if the
    trust policy of the role being assumed includes a condition that
    requires MFA authentication. The value is either the serial number for a
    hardware device (such as `GAHT12345678`) or an Amazon Resource Name
    (ARN) for a virtual device (such as
    `arn:aws:iam::123456789012:mfa/user`).

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: +=/:,.@-
    """

    token_code: str | None = None
    """
    The value provided by the MFA device, if the trust policy of the role
    being assumed requires MFA. (In other words, if the policy includes a
    condition that tests for MFA). If the role being assumed requires MFA
    and if the `TokenCode` value is missing or expired, the `AssumeRole`
    call returns an \"access denied\" error.

    The format for this parameter, as described by its regex pattern, is a
    sequence of six numeric digits.
    """

    source_identity: str | None = None
    """
    The source identity specified by the principal that is calling the
    `AssumeRole` operation. The source identity value persists across
    [chained
    role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles.html#iam-term-role-chaining)
    sessions.

    You can require users to specify a source identity when they assume a
    role. You do this by using the
    [`sts:SourceIdentity`](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_condition-keys.html#condition-keys-sourceidentity)
    condition key in a role trust policy. You can use source identity
    information in CloudTrail logs to determine who took actions with a
    role. You can use the `aws:SourceIdentity` condition key to further
    control access to Amazon Web Services resources based on the value of
    source identity. For more information about using source identity, see
    [Monitor and control actions taken with assumed
    roles](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_control-access_monitor.html)
    in the *IAM User Guide*.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: +=,.@-. You cannot use a value that begins with the text
    `aws:`. This prefix is reserved for Amazon Web Services internal use.
    """

    provided_contexts: list[ProvidedContext] | None = None
    """
    A list of previously acquired trusted context assertions in the format
    of a JSON array. The trusted context assertion is signed and encrypted
    by Amazon Web Services STS.

    The following is an example of a `ProvidedContext` value that includes a
    single trusted context assertion and the ARN of the context provider
    from which the trusted context assertion was generated.

    `[{"ProviderArn":"arn:aws:iam::aws:contextProvider/IdentityCenter","ContextAssertion":"trusted-context-assertion"}]`
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.role_arn is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_INPUT.members["RoleArn"], self.role_arn
            )

        if self.role_session_name is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_INPUT.members["RoleSessionName"],
                self.role_session_name,
            )

        if self.policy_arns is not None:
            _serialize_policy_descriptor_list_type(
                serializer,
                _SCHEMA_ASSUME_ROLE_INPUT.members["PolicyArns"],
                self.policy_arns,
            )

        if self.policy is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_INPUT.members["Policy"], self.policy
            )

        if self.duration_seconds is not None:
            serializer.write_integer(
                _SCHEMA_ASSUME_ROLE_INPUT.members["DurationSeconds"],
                self.duration_seconds,
            )

        if self.tags is not None:
            _serialize_tag_list_type(
                serializer, _SCHEMA_ASSUME_ROLE_INPUT.members["Tags"], self.tags
            )

        if self.transitive_tag_keys is not None:
            _serialize_tag_key_list_type(
                serializer,
                _SCHEMA_ASSUME_ROLE_INPUT.members["TransitiveTagKeys"],
                self.transitive_tag_keys,
            )

        if self.external_id is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_INPUT.members["ExternalId"], self.external_id
            )

        if self.serial_number is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_INPUT.members["SerialNumber"], self.serial_number
            )

        if self.token_code is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_INPUT.members["TokenCode"], self.token_code
            )

        if self.source_identity is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_INPUT.members["SourceIdentity"],
                self.source_identity,
            )

        if self.provided_contexts is not None:
            _serialize_provided_contexts_list_type(
                serializer,
                _SCHEMA_ASSUME_ROLE_INPUT.members["ProvidedContexts"],
                self.provided_contexts,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["role_arn"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["RoleArn"]
                    )

                case 1:
                    kwargs["role_session_name"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["RoleSessionName"]
                    )

                case 2:
                    kwargs["policy_arns"] = _deserialize_policy_descriptor_list_type(
                        de, _SCHEMA_ASSUME_ROLE_INPUT.members["PolicyArns"]
                    )

                case 3:
                    kwargs["policy"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["Policy"]
                    )

                case 4:
                    kwargs["duration_seconds"] = de.read_integer(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["DurationSeconds"]
                    )

                case 5:
                    kwargs["tags"] = _deserialize_tag_list_type(
                        de, _SCHEMA_ASSUME_ROLE_INPUT.members["Tags"]
                    )

                case 6:
                    kwargs["transitive_tag_keys"] = _deserialize_tag_key_list_type(
                        de, _SCHEMA_ASSUME_ROLE_INPUT.members["TransitiveTagKeys"]
                    )

                case 7:
                    kwargs["external_id"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["ExternalId"]
                    )

                case 8:
                    kwargs["serial_number"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["SerialNumber"]
                    )

                case 9:
                    kwargs["token_code"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["TokenCode"]
                    )

                case 10:
                    kwargs["source_identity"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_INPUT.members["SourceIdentity"]
                    )

                case 11:
                    kwargs["provided_contexts"] = (
                        _deserialize_provided_contexts_list_type(
                            de, _SCHEMA_ASSUME_ROLE_INPUT.members["ProvidedContexts"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ASSUME_ROLE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class Credentials:
    """Amazon Web Services credentials for API authentication."""

    access_key_id: str
    """The access key ID that identifies the temporary security credentials."""

    secret_access_key: str = field(repr=False)
    """The secret access key that can be used to sign requests."""

    session_token: str
    """
    The token that users must pass to the service API to use the temporary
    credentials.
    """

    expiration: datetime
    """The date on which the current credentials expire."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREDENTIALS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CREDENTIALS.members["AccessKeyId"], self.access_key_id
        )
        serializer.write_string(
            _SCHEMA_CREDENTIALS.members["SecretAccessKey"], self.secret_access_key
        )
        serializer.write_string(
            _SCHEMA_CREDENTIALS.members["SessionToken"], self.session_token
        )
        serializer.write_timestamp(
            _SCHEMA_CREDENTIALS.members["Expiration"], self.expiration
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["access_key_id"] = de.read_string(
                        _SCHEMA_CREDENTIALS.members["AccessKeyId"]
                    )

                case 1:
                    kwargs["secret_access_key"] = de.read_string(
                        _SCHEMA_CREDENTIALS.members["SecretAccessKey"]
                    )

                case 2:
                    kwargs["session_token"] = de.read_string(
                        _SCHEMA_CREDENTIALS.members["SessionToken"]
                    )

                case 3:
                    kwargs["expiration"] = de.read_timestamp(
                        _SCHEMA_CREDENTIALS.members["Expiration"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREDENTIALS, consumer=_consumer)
        if "access_key_id" not in kwargs:
            kwargs["access_key_id"] = ""
        if "secret_access_key" not in kwargs:
            kwargs["secret_access_key"] = ""
        if "session_token" not in kwargs:
            kwargs["session_token"] = ""
        if "expiration" not in kwargs:
            kwargs["expiration"] = datetime.fromtimestamp(0, tz=timezone.utc)
        return kwargs


@dataclass(kw_only=True)
class AssumeRoleOutput:
    """
    Contains the response to a successful AssumeRole request, including
    temporary Amazon Web Services credentials that can be used to make
    Amazon Web Services requests.
    """

    credentials: Credentials | None = None
    """
    The temporary security credentials, which include an access key ID, a
    secret access key, and a security (or session) token.

    Note:
        The size of the security token that STS API operations return is not
        fixed. We strongly recommend that you make no assumptions about the
        maximum size.
    """

    assumed_role_user: AssumedRoleUser | None = None
    """
    The Amazon Resource Name (ARN) and the assumed role ID, which are
    identifiers that you can use to refer to the resulting temporary
    security credentials. For example, you can reference these credentials
    as a principal in a resource-based policy by using the ARN or assumed
    role ID. The ARN and ID include the `RoleSessionName` that you specified
    when you called `AssumeRole`.
    """

    packed_policy_size: int | None = None
    """
    A percentage value that indicates the packed size of the session
    policies and session tags combined passed in the request. The request
    fails if the packed size is greater than 100 percent, which means the
    policies and tags exceeded the allowed space.
    """

    source_identity: str | None = None
    """
    The source identity specified by the principal that is calling the
    `AssumeRole` operation.

    You can require users to specify a source identity when they assume a
    role. You do this by using the `sts:SourceIdentity` condition key in a
    role trust policy. You can use source identity information in CloudTrail
    logs to determine who took actions with a role. You can use the
    `aws:SourceIdentity` condition key to further control access to Amazon
    Web Services resources based on the value of source identity. For more
    information about using source identity, see [Monitor and control
    actions taken with assumed
    roles](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_control-access_monitor.html)
    in the *IAM User Guide*.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: =,.@-
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.credentials is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROLE_OUTPUT.members["Credentials"], self.credentials
            )

        if self.assumed_role_user is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROLE_OUTPUT.members["AssumedRoleUser"],
                self.assumed_role_user,
            )

        if self.packed_policy_size is not None:
            serializer.write_integer(
                _SCHEMA_ASSUME_ROLE_OUTPUT.members["PackedPolicySize"],
                self.packed_policy_size,
            )

        if self.source_identity is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_OUTPUT.members["SourceIdentity"],
                self.source_identity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["credentials"] = Credentials.deserialize(de)

                case 1:
                    kwargs["assumed_role_user"] = AssumedRoleUser.deserialize(de)

                case 2:
                    kwargs["packed_policy_size"] = de.read_integer(
                        _SCHEMA_ASSUME_ROLE_OUTPUT.members["PackedPolicySize"]
                    )

                case 3:
                    kwargs["source_identity"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_OUTPUT.members["SourceIdentity"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ASSUME_ROLE_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ExpiredTokenException(ServiceError):
    """
    The web identity token that was passed is expired or is not valid. Get a
    new identity token from the identity provider and then retry the
    request.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPIRED_TOKEN_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_EXPIRED_TOKEN_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_EXPIRED_TOKEN_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXPIRED_TOKEN_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class MalformedPolicyDocumentException(ServiceError):
    """
    The request was rejected because the policy document was malformed. The
    error message describes the specific error.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class PackedPolicyTooLargeException(ServiceError):
    """
    The request was rejected because the total packed size of the session
    policies and session tags combined was too large. An Amazon Web Services
    conversion compresses the session policy document, session policy ARNs,
    and session tags into a packed binary format that has a separate limit.
    The error message indicates by percentage how close the policies and
    tags are to the upper size limit. For more information, see [Passing
    Session Tags in
    STS](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_session-tags.html)
    in the *IAM User Guide*.

    You could receive this error even though you meet other defined session
    policy and session tag limits. For more information, see [IAM and STS
    Entity Character
    Limits](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-quotas.html#reference_iam-limits-entity-length)
    in the *IAM User Guide*.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class RegionDisabledException(ServiceError):
    """
    STS is not activated in the requested region for the account that is
    being asked to generate credentials. The account administrator must use
    the IAM console to activate STS in that region. For more information,
    see [Activating and Deactivating STS in an Amazon Web Services
    Region](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_enable-regions.html#sts-regions-activate-deactivate)
    in the *IAM User Guide*.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REGION_DISABLED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_REGION_DISABLED_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_REGION_DISABLED_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REGION_DISABLED_EXCEPTION, consumer=_consumer)
        return kwargs


ASSUME_ROLE = APIOperation(
    input=AssumeRoleInput,
    output=AssumeRoleOutput,
    schema=_SCHEMA_ASSUME_ROLE,
    input_schema=_SCHEMA_ASSUME_ROLE_INPUT,
    output_schema=_SCHEMA_ASSUME_ROLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sts#ExpiredTokenException"): ExpiredTokenException,
            ShapeID(
                "com.amazonaws.sts#MalformedPolicyDocumentException"
            ): MalformedPolicyDocumentException,
            ShapeID(
                "com.amazonaws.sts#PackedPolicyTooLargeException"
            ): PackedPolicyTooLargeException,
            ShapeID(
                "com.amazonaws.sts#RegionDisabledException"
            ): RegionDisabledException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[
        _SCHEMA_EXPIRED_TOKEN_EXCEPTION,
        _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION,
        _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION,
        _SCHEMA_REGION_DISABLED_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class AssumeRoleWithSAMLInput:
    """Dataclass for AssumeRoleWithSAMLInput structure."""

    role_arn: str | None = None
    """The Amazon Resource Name (ARN) of the role that the caller is assuming."""

    principal_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the SAML provider in IAM that
    describes the IdP.
    """

    saml_assertion: str | None = field(repr=False, default=None)
    """
    The base64 encoded SAML authentication response provided by the IdP.

    For more information, see [Configuring a Relying Party and Adding
    Claims](https://docs.aws.amazon.com/IAM/latest/UserGuide/create-role-saml-IdP-tasks.html)
    in the *IAM User Guide*.
    """

    policy_arns: list[PolicyDescriptorType] | None = None
    """
    The Amazon Resource Names (ARNs) of the IAM managed policies that you
    want to use as managed session policies. The policies must exist in the
    same account as the role.

    This parameter is optional. You can provide up to 10 managed policy
    ARNs. However, the plaintext that you use for both inline and managed
    session policies can't exceed 2,048 characters. For more information
    about ARNs, see [Amazon Resource Names (ARNs) and Amazon Web Services
    Service
    Namespaces](https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
    in the Amazon Web Services General Reference.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.

    Passing policies to this operation returns new temporary credentials.
    The resulting session's permissions are the intersection of the role's
    identity-based policy and the session policies. You can use the role's
    temporary credentials in subsequent Amazon Web Services API calls to
    access resources in the account that owns the role. You cannot use
    session policies to grant more permissions than those allowed by the
    identity-based policy of the role that is being assumed. For more
    information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.
    """

    policy: str | None = None
    """
    An IAM policy in JSON format that you want to use as an inline session
    policy.

    This parameter is optional. Passing policies to this operation returns
    new temporary credentials. The resulting session's permissions are the
    intersection of the role's identity-based policy and the session
    policies. You can use the role's temporary credentials in subsequent
    Amazon Web Services API calls to access resources in the account that
    owns the role. You cannot use session policies to grant more permissions
    than those allowed by the identity-based policy of the role that is
    being assumed. For more information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.

    The plaintext that you use for both inline and managed session policies
    can't exceed 2,048 characters. The JSON policy characters can be any
    ASCII character from the space character to the end of the valid
    character list (\\u0020 through \\u00FF). It can also include the tab
    (\\u0009), linefeed (\\u000A), and carriage return (\\u000D) characters.

    For more information about role session permissions, see [Session
    policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session).

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.
    """

    duration_seconds: int | None = None
    """
    The duration, in seconds, of the role session. Your role session lasts
    for the duration that you specify for the `DurationSeconds` parameter,
    or until the time specified in the SAML authentication response's
    `SessionNotOnOrAfter` value, whichever is shorter. You can provide a
    `DurationSeconds` value from 900 seconds (15 minutes) up to the maximum
    session duration setting for the role. This setting can have a value
    from 1 hour to 12 hours. If you specify a value higher than this
    setting, the operation fails. For example, if you specify a session
    duration of 12 hours, but your administrator set the maximum session
    duration to 6 hours, your operation fails. To learn how to view the
    maximum value for your role, see [View the Maximum Session Duration
    Setting for a
    Role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use.html#id_roles_use_view-role-max-session)
    in the *IAM User Guide*.

    By default, the value is set to `3600` seconds.

    Note:
        The `DurationSeconds` parameter is separate from the duration of a
        console session that you might request using the returned credentials.
        The request to the federation endpoint for a console sign-in token takes
        a `SessionDuration` parameter that specifies the maximum length of the
        console session. For more information, see [Creating a URL that Enables
        Federated Users to Access the Amazon Web Services Management
        Console](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_enable-console-custom-url.html)
        in the *IAM User Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.role_arn is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["RoleArn"], self.role_arn
            )

        if self.principal_arn is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["PrincipalArn"],
                self.principal_arn,
            )

        if self.saml_assertion is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["SAMLAssertion"],
                self.saml_assertion,
            )

        if self.policy_arns is not None:
            _serialize_policy_descriptor_list_type(
                serializer,
                _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["PolicyArns"],
                self.policy_arns,
            )

        if self.policy is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["Policy"], self.policy
            )

        if self.duration_seconds is not None:
            serializer.write_integer(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["DurationSeconds"],
                self.duration_seconds,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["role_arn"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["RoleArn"]
                    )

                case 1:
                    kwargs["principal_arn"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["PrincipalArn"]
                    )

                case 2:
                    kwargs["saml_assertion"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["SAMLAssertion"]
                    )

                case 3:
                    kwargs["policy_arns"] = _deserialize_policy_descriptor_list_type(
                        de, _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["PolicyArns"]
                    )

                case 4:
                    kwargs["policy"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["Policy"]
                    )

                case 5:
                    kwargs["duration_seconds"] = de.read_integer(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT.members["DurationSeconds"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class AssumeRoleWithSAMLOutput:
    """
    Contains the response to a successful AssumeRoleWithSAML request,
    including temporary Amazon Web Services credentials that can be used to
    make Amazon Web Services requests.
    """

    credentials: Credentials | None = None
    """
    The temporary security credentials, which include an access key ID, a
    secret access key, and a security (or session) token.

    Note:
        The size of the security token that STS API operations return is not
        fixed. We strongly recommend that you make no assumptions about the
        maximum size.
    """

    assumed_role_user: AssumedRoleUser | None = None
    """
    The identifiers for the temporary security credentials that the
    operation returns.
    """

    packed_policy_size: int | None = None
    """
    A percentage value that indicates the packed size of the session
    policies and session tags combined passed in the request. The request
    fails if the packed size is greater than 100 percent, which means the
    policies and tags exceeded the allowed space.
    """

    subject: str | None = None
    """
    The value of the `NameID` element in the `Subject` element of the SAML
    assertion.
    """

    subject_type: str | None = None
    """
    The format of the name ID, as defined by the `Format` attribute in the
    `NameID` element of the SAML assertion. Typical examples of the format
    are `transient` or `persistent`.

    If the format includes the prefix
    `urn:oasis:names:tc:SAML:2.0:nameid-format`, that prefix is removed. For
    example, `urn:oasis:names:tc:SAML:2.0:nameid-format:transient` is
    returned as `transient`. If the format includes any other prefix, the
    format is returned with no modifications.
    """

    issuer: str | None = None
    """The value of the `Issuer` element of the SAML assertion."""

    audience: str | None = None
    """
    The value of the `Recipient` attribute of the `SubjectConfirmationData`
    element of the SAML assertion.
    """

    name_qualifier: str | None = None
    """
    A hash value based on the concatenation of the following:

    - The `Issuer` response value.

    - The Amazon Web Services account ID.

    - The friendly name (the last part of the ARN) of the SAML provider in
      IAM.

    The combination of `NameQualifier` and `Subject` can be used to uniquely
    identify a user.

    The following pseudocode shows how the hash value is calculated:

    `BASE64 ( SHA1 ( "https://example.com/saml" + "123456789012" + "/MySAMLIdP" ) )`
    """

    source_identity: str | None = None
    """
    The value in the `SourceIdentity` attribute in the SAML assertion. The
    source identity value persists across [chained
    role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles.html#iam-term-role-chaining)
    sessions.

    You can require users to set a source identity value when they assume a
    role. You do this by using the `sts:SourceIdentity` condition key in a
    role trust policy. That way, actions that are taken with the role are
    associated with that user. After the source identity is set, the value
    cannot be changed. It is present in the request for all actions that are
    taken by the role and persists across [chained
    role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles.html#id_roles_terms-and-concepts)
    sessions. You can configure your SAML identity provider to use an
    attribute associated with your users, like user name or email, as the
    source identity when calling `AssumeRoleWithSAML`. You do this by adding
    an attribute to the SAML assertion. For more information about using
    source identity, see [Monitor and control actions taken with assumed
    roles](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_control-access_monitor.html)
    in the *IAM User Guide*.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: =,.@-
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.credentials is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["Credentials"],
                self.credentials,
            )

        if self.assumed_role_user is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["AssumedRoleUser"],
                self.assumed_role_user,
            )

        if self.packed_policy_size is not None:
            serializer.write_integer(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["PackedPolicySize"],
                self.packed_policy_size,
            )

        if self.subject is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["Subject"], self.subject
            )

        if self.subject_type is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["SubjectType"],
                self.subject_type,
            )

        if self.issuer is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["Issuer"], self.issuer
            )

        if self.audience is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["Audience"], self.audience
            )

        if self.name_qualifier is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["NameQualifier"],
                self.name_qualifier,
            )

        if self.source_identity is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["SourceIdentity"],
                self.source_identity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["credentials"] = Credentials.deserialize(de)

                case 1:
                    kwargs["assumed_role_user"] = AssumedRoleUser.deserialize(de)

                case 2:
                    kwargs["packed_policy_size"] = de.read_integer(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["PackedPolicySize"]
                    )

                case 3:
                    kwargs["subject"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["Subject"]
                    )

                case 4:
                    kwargs["subject_type"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["SubjectType"]
                    )

                case 5:
                    kwargs["issuer"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["Issuer"]
                    )

                case 6:
                    kwargs["audience"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["Audience"]
                    )

                case 7:
                    kwargs["name_qualifier"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["NameQualifier"]
                    )

                case 8:
                    kwargs["source_identity"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT.members["SourceIdentity"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class IDPRejectedClaimException(ServiceError):
    """
    The identity provider (IdP) reported that authentication failed. This
    might be because the claim is invalid.

    If this error is returned for the `AssumeRoleWithWebIdentity` operation,
    it can also mean that the claim has expired or has been explicitly
    revoked.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IDP_REJECTED_CLAIM_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_IDP_REJECTED_CLAIM_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_IDP_REJECTED_CLAIM_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_IDP_REJECTED_CLAIM_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class InvalidIdentityTokenException(ServiceError):
    """
    The web identity token that was passed could not be validated by Amazon
    Web Services. Get a new identity token from the identity provider and
    then retry the request.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_IDENTITY_TOKEN_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_IDENTITY_TOKEN_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_IDENTITY_TOKEN_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_INVALID_IDENTITY_TOKEN_EXCEPTION, consumer=_consumer
        )
        return kwargs


ASSUME_ROLE_WITH_SAML = APIOperation(
    input=AssumeRoleWithSAMLInput,
    output=AssumeRoleWithSAMLOutput,
    schema=_SCHEMA_ASSUME_ROLE_WITH_SAML,
    input_schema=_SCHEMA_ASSUME_ROLE_WITH_SAML_INPUT,
    output_schema=_SCHEMA_ASSUME_ROLE_WITH_SAML_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sts#ExpiredTokenException"): ExpiredTokenException,
            ShapeID(
                "com.amazonaws.sts#IDPRejectedClaimException"
            ): IDPRejectedClaimException,
            ShapeID(
                "com.amazonaws.sts#InvalidIdentityTokenException"
            ): InvalidIdentityTokenException,
            ShapeID(
                "com.amazonaws.sts#MalformedPolicyDocumentException"
            ): MalformedPolicyDocumentException,
            ShapeID(
                "com.amazonaws.sts#PackedPolicyTooLargeException"
            ): PackedPolicyTooLargeException,
            ShapeID(
                "com.amazonaws.sts#RegionDisabledException"
            ): RegionDisabledException,
        }
    ),
    effective_auth_schemes=[ShapeID("smithy.api#noAuth")],
    error_schemas=[
        _SCHEMA_EXPIRED_TOKEN_EXCEPTION,
        _SCHEMA_IDP_REJECTED_CLAIM_EXCEPTION,
        _SCHEMA_INVALID_IDENTITY_TOKEN_EXCEPTION,
        _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION,
        _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION,
        _SCHEMA_REGION_DISABLED_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class AssumeRoleWithWebIdentityInput:
    """Dataclass for AssumeRoleWithWebIdentityInput structure."""

    role_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the role that the caller is assuming.

    Note:
        Additional considerations apply to Amazon Cognito identity pools that
        assume [cross-account IAM
        roles](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies-cross-account-resource-access.html).
        The trust policies of these roles must accept the
        `cognito-identity.amazonaws.com` service principal and must contain the
        `cognito-identity.amazonaws.com:aud` condition key to restrict role
        assumption to users from your intended identity pools. A policy that
        trusts Amazon Cognito identity pools without this condition creates a
        risk that a user from an unintended identity pool can assume the role.
        For more information, see [Trust policies for IAM roles in Basic
        (Classic)
        authentication](https://docs.aws.amazon.com/cognito/latest/developerguide/iam-roles.html#trust-policies)
        in the *Amazon Cognito Developer Guide*.
    """

    role_session_name: str | None = None
    """
    An identifier for the assumed role session. Typically, you pass the name
    or identifier that is associated with the user who is using your
    application. That way, the temporary security credentials that your
    application will use are associated with that user. This session name is
    included as part of the ARN and assumed role ID in the `AssumedRoleUser`
    response element.

    For security purposes, administrators can view this field in [CloudTrail
    logs](https://docs.aws.amazon.com/IAM/latest/UserGuide/cloudtrail-integration.html#cloudtrail-integration_signin-tempcreds)
    to help identify who performed an action in Amazon Web Services. Your
    administrator might require that you specify your user name as the
    session name when you assume the role. For more information, see
    [`sts:RoleSessionName`](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_iam-condition-keys.html#ck_rolesessionname)
    .

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: =,.@-
    """

    web_identity_token: str | None = field(repr=False, default=None)
    """
    The OAuth 2.0 access token or OpenID Connect ID token that is provided
    by the identity provider. Your application must get this token by
    authenticating the user who is using your application with a web
    identity provider before the application makes an
    `AssumeRoleWithWebIdentity` call. Timestamps in the token must be
    formatted as either an integer or a long integer. Tokens must be signed
    using either RSA keys (RS256, RS384, or RS512) or ECDSA keys (ES256,
    ES384, or ES512).
    """

    provider_id: str | None = None
    """
    The fully qualified host component of the domain name of the OAuth 2.0
    identity provider. Do not specify this value for an OpenID Connect
    identity provider.

    Currently `www.amazon.com` and `graph.facebook.com` are the only
    supported identity providers for OAuth 2.0 access tokens. Do not include
    URL schemes and port numbers.

    Do not specify this value for OpenID Connect ID tokens.
    """

    policy_arns: list[PolicyDescriptorType] | None = None
    """
    The Amazon Resource Names (ARNs) of the IAM managed policies that you
    want to use as managed session policies. The policies must exist in the
    same account as the role.

    This parameter is optional. You can provide up to 10 managed policy
    ARNs. However, the plaintext that you use for both inline and managed
    session policies can't exceed 2,048 characters. For more information
    about ARNs, see [Amazon Resource Names (ARNs) and Amazon Web Services
    Service
    Namespaces](https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
    in the Amazon Web Services General Reference.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.

    Passing policies to this operation returns new temporary credentials.
    The resulting session's permissions are the intersection of the role's
    identity-based policy and the session policies. You can use the role's
    temporary credentials in subsequent Amazon Web Services API calls to
    access resources in the account that owns the role. You cannot use
    session policies to grant more permissions than those allowed by the
    identity-based policy of the role that is being assumed. For more
    information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.
    """

    policy: str | None = None
    """
    An IAM policy in JSON format that you want to use as an inline session
    policy.

    This parameter is optional. Passing policies to this operation returns
    new temporary credentials. The resulting session's permissions are the
    intersection of the role's identity-based policy and the session
    policies. You can use the role's temporary credentials in subsequent
    Amazon Web Services API calls to access resources in the account that
    owns the role. You cannot use session policies to grant more permissions
    than those allowed by the identity-based policy of the role that is
    being assumed. For more information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.

    The plaintext that you use for both inline and managed session policies
    can't exceed 2,048 characters. The JSON policy characters can be any
    ASCII character from the space character to the end of the valid
    character list (\\u0020 through \\u00FF). It can also include the tab
    (\\u0009), linefeed (\\u000A), and carriage return (\\u000D) characters.

    For more information about role session permissions, see [Session
    policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session).

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.
    """

    duration_seconds: int | None = None
    """
    The duration, in seconds, of the role session. The value can range from
    900 seconds (15 minutes) up to the maximum session duration setting for
    the role. This setting can have a value from 1 hour to 12 hours. If you
    specify a value higher than this setting, the operation fails. For
    example, if you specify a session duration of 12 hours, but your
    administrator set the maximum session duration to 6 hours, your
    operation fails. To learn how to view the maximum value for your role,
    see [View the Maximum Session Duration Setting for a
    Role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use.html#id_roles_use_view-role-max-session)
    in the *IAM User Guide*.

    By default, the value is set to `3600` seconds.

    Note:
        The `DurationSeconds` parameter is separate from the duration of a
        console session that you might request using the returned credentials.
        The request to the federation endpoint for a console sign-in token takes
        a `SessionDuration` parameter that specifies the maximum length of the
        console session. For more information, see [Creating a URL that Enables
        Federated Users to Access the Amazon Web Services Management
        Console](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_enable-console-custom-url.html)
        in the *IAM User Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.role_arn is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["RoleArn"],
                self.role_arn,
            )

        if self.role_session_name is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["RoleSessionName"],
                self.role_session_name,
            )

        if self.web_identity_token is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["WebIdentityToken"],
                self.web_identity_token,
            )

        if self.provider_id is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["ProviderId"],
                self.provider_id,
            )

        if self.policy_arns is not None:
            _serialize_policy_descriptor_list_type(
                serializer,
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["PolicyArns"],
                self.policy_arns,
            )

        if self.policy is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["Policy"],
                self.policy,
            )

        if self.duration_seconds is not None:
            serializer.write_integer(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["DurationSeconds"],
                self.duration_seconds,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["role_arn"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["RoleArn"]
                    )

                case 1:
                    kwargs["role_session_name"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members[
                            "RoleSessionName"
                        ]
                    )

                case 2:
                    kwargs["web_identity_token"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members[
                            "WebIdentityToken"
                        ]
                    )

                case 3:
                    kwargs["provider_id"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members[
                            "ProviderId"
                        ]
                    )

                case 4:
                    kwargs["policy_arns"] = _deserialize_policy_descriptor_list_type(
                        de,
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members[
                            "PolicyArns"
                        ],
                    )

                case 5:
                    kwargs["policy"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members["Policy"]
                    )

                case 6:
                    kwargs["duration_seconds"] = de.read_integer(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT.members[
                            "DurationSeconds"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class AssumeRoleWithWebIdentityOutput:
    """
    Contains the response to a successful AssumeRoleWithWebIdentity request,
    including temporary Amazon Web Services credentials that can be used to
    make Amazon Web Services requests.
    """

    credentials: Credentials | None = None
    """
    The temporary security credentials, which include an access key ID, a
    secret access key, and a security token.

    Note:
        The size of the security token that STS API operations return is not
        fixed. We strongly recommend that you make no assumptions about the
        maximum size.
    """

    subject_from_web_identity_token: str | None = None
    """
    The unique user identifier that is returned by the identity provider.
    This identifier is associated with the `WebIdentityToken` that was
    submitted with the `AssumeRoleWithWebIdentity` call. The identifier is
    typically unique to the user and the application that acquired the
    `WebIdentityToken` (pairwise identifier). For OpenID Connect ID tokens,
    this field contains the value returned by the identity provider as the
    token's `sub` (Subject) claim.
    """

    assumed_role_user: AssumedRoleUser | None = None
    """
    The Amazon Resource Name (ARN) and the assumed role ID, which are
    identifiers that you can use to refer to the resulting temporary
    security credentials. For example, you can reference these credentials
    as a principal in a resource-based policy by using the ARN or assumed
    role ID. The ARN and ID include the `RoleSessionName` that you specified
    when you called `AssumeRole`.
    """

    packed_policy_size: int | None = None
    """
    A percentage value that indicates the packed size of the session
    policies and session tags combined passed in the request. The request
    fails if the packed size is greater than 100 percent, which means the
    policies and tags exceeded the allowed space.
    """

    provider: str | None = None
    """
    The issuing authority of the web identity token presented. For OpenID
    Connect ID tokens, this contains the value of the `iss` field. For OAuth
    2.0 access tokens, this contains the value of the `ProviderId` parameter
    that was passed in the `AssumeRoleWithWebIdentity` request.
    """

    audience: str | None = None
    """
    The intended audience (also known as client ID) of the web identity
    token. This is traditionally the client identifier issued to the
    application that requested the web identity token.
    """

    source_identity: str | None = None
    """
    The value of the source identity that is returned in the JSON web token
    (JWT) from the identity provider.

    You can require users to set a source identity value when they assume a
    role. You do this by using the `sts:SourceIdentity` condition key in a
    role trust policy. That way, actions that are taken with the role are
    associated with that user. After the source identity is set, the value
    cannot be changed. It is present in the request for all actions that are
    taken by the role and persists across [chained
    role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles.html#id_roles_terms-and-concepts)
    sessions. You can configure your identity provider to use an attribute
    associated with your users, like user name or email, as the source
    identity when calling `AssumeRoleWithWebIdentity`. You do this by adding
    a claim to the JSON web token. To learn more about OIDC tokens and
    claims, see [Using Tokens with User
    Pools](https://docs.aws.amazon.com/cognito/latest/developerguide/amazon-cognito-user-pools-using-tokens-with-identity-providers.html)
    in the *Amazon Cognito Developer Guide*. For more information about
    using source identity, see [Monitor and control actions taken with
    assumed
    roles](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_control-access_monitor.html)
    in the *IAM User Guide*.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: =,.@-
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.credentials is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members["Credentials"],
                self.credentials,
            )

        if self.subject_from_web_identity_token is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members[
                    "SubjectFromWebIdentityToken"
                ],
                self.subject_from_web_identity_token,
            )

        if self.assumed_role_user is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members["AssumedRoleUser"],
                self.assumed_role_user,
            )

        if self.packed_policy_size is not None:
            serializer.write_integer(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members[
                    "PackedPolicySize"
                ],
                self.packed_policy_size,
            )

        if self.provider is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members["Provider"],
                self.provider,
            )

        if self.audience is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members["Audience"],
                self.audience,
            )

        if self.source_identity is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members["SourceIdentity"],
                self.source_identity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["credentials"] = Credentials.deserialize(de)

                case 1:
                    kwargs["subject_from_web_identity_token"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members[
                            "SubjectFromWebIdentityToken"
                        ]
                    )

                case 2:
                    kwargs["assumed_role_user"] = AssumedRoleUser.deserialize(de)

                case 3:
                    kwargs["packed_policy_size"] = de.read_integer(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members[
                            "PackedPolicySize"
                        ]
                    )

                case 4:
                    kwargs["provider"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members["Provider"]
                    )

                case 5:
                    kwargs["audience"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members["Audience"]
                    )

                case 6:
                    kwargs["source_identity"] = de.read_string(
                        _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT.members[
                            "SourceIdentity"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class IDPCommunicationErrorException(ServiceError):
    """
    The request could not be fulfilled because the identity provider (IDP)
    that was asked to verify the incoming identity token could not be
    reached. This is often a transient error caused by network conditions.
    Retry the request a limited number of times so that you don't exceed
    the request rate. If the error persists, the identity provider might be
    down or not responding.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IDP_COMMUNICATION_ERROR_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_IDP_COMMUNICATION_ERROR_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_IDP_COMMUNICATION_ERROR_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_IDP_COMMUNICATION_ERROR_EXCEPTION, consumer=_consumer
        )
        return kwargs


ASSUME_ROLE_WITH_WEB_IDENTITY = APIOperation(
    input=AssumeRoleWithWebIdentityInput,
    output=AssumeRoleWithWebIdentityOutput,
    schema=_SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY,
    input_schema=_SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_INPUT,
    output_schema=_SCHEMA_ASSUME_ROLE_WITH_WEB_IDENTITY_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sts#ExpiredTokenException"): ExpiredTokenException,
            ShapeID(
                "com.amazonaws.sts#IDPCommunicationErrorException"
            ): IDPCommunicationErrorException,
            ShapeID(
                "com.amazonaws.sts#IDPRejectedClaimException"
            ): IDPRejectedClaimException,
            ShapeID(
                "com.amazonaws.sts#InvalidIdentityTokenException"
            ): InvalidIdentityTokenException,
            ShapeID(
                "com.amazonaws.sts#MalformedPolicyDocumentException"
            ): MalformedPolicyDocumentException,
            ShapeID(
                "com.amazonaws.sts#PackedPolicyTooLargeException"
            ): PackedPolicyTooLargeException,
            ShapeID(
                "com.amazonaws.sts#RegionDisabledException"
            ): RegionDisabledException,
        }
    ),
    effective_auth_schemes=[ShapeID("smithy.api#noAuth")],
    error_schemas=[
        _SCHEMA_EXPIRED_TOKEN_EXCEPTION,
        _SCHEMA_IDP_COMMUNICATION_ERROR_EXCEPTION,
        _SCHEMA_IDP_REJECTED_CLAIM_EXCEPTION,
        _SCHEMA_INVALID_IDENTITY_TOKEN_EXCEPTION,
        _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION,
        _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION,
        _SCHEMA_REGION_DISABLED_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class AssumeRootInput:
    """Dataclass for AssumeRootInput structure."""

    target_principal: str | None = None
    """The member account principal ARN or account ID."""

    task_policy_arn: PolicyDescriptorType | None = None
    """
    The identity based policy that scopes the session to the privileged
    tasks that can be performed. You must use one of following Amazon Web
    Services managed policies to scope root session actions:

    - [IAMAuditRootUserCredentials](https://docs.aws.amazon.com/IAM/latest/UserGuide/security-iam-awsmanpol.html#security-iam-awsmanpol-IAMAuditRootUserCredentials)

    - [IAMCreateRootUserPassword](https://docs.aws.amazon.com/IAM/latest/UserGuide/security-iam-awsmanpol.html#security-iam-awsmanpol-IAMCreateRootUserPassword)

    - [IAMDeleteRootUserCredentials](https://docs.aws.amazon.com/IAM/latest/UserGuide/security-iam-awsmanpol.html#security-iam-awsmanpol-IAMDeleteRootUserCredentials)

    - [S3UnlockBucketPolicy](https://docs.aws.amazon.com/IAM/latest/UserGuide/security-iam-awsmanpol.html#security-iam-awsmanpol-S3UnlockBucketPolicy)

    - [SQSUnlockQueuePolicy](https://docs.aws.amazon.com/IAM/latest/UserGuide/security-iam-awsmanpol.html#security-iam-awsmanpol-SQSUnlockQueuePolicy)
    """

    duration_seconds: int | None = None
    """
    The duration, in seconds, of the privileged session. The value can range
    from 0 seconds up to the maximum session duration of 900 seconds (15
    minutes). If you specify a value higher than this setting, the operation
    fails.

    By default, the value is set to `900` seconds.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROOT_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.target_principal is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROOT_INPUT.members["TargetPrincipal"],
                self.target_principal,
            )

        if self.task_policy_arn is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROOT_INPUT.members["TaskPolicyArn"], self.task_policy_arn
            )

        if self.duration_seconds is not None:
            serializer.write_integer(
                _SCHEMA_ASSUME_ROOT_INPUT.members["DurationSeconds"],
                self.duration_seconds,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["target_principal"] = de.read_string(
                        _SCHEMA_ASSUME_ROOT_INPUT.members["TargetPrincipal"]
                    )

                case 1:
                    kwargs["task_policy_arn"] = PolicyDescriptorType.deserialize(de)

                case 2:
                    kwargs["duration_seconds"] = de.read_integer(
                        _SCHEMA_ASSUME_ROOT_INPUT.members["DurationSeconds"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ASSUME_ROOT_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class AssumeRootOutput:
    """Dataclass for AssumeRootOutput structure."""

    credentials: Credentials | None = None
    """
    The temporary security credentials, which include an access key ID, a
    secret access key, and a security token.

    Note:
        The size of the security token that STS API operations return is not
        fixed. We strongly recommend that you make no assumptions about the
        maximum size.
    """

    source_identity: str | None = None
    """
    The source identity specified by the principal that is calling the
    `AssumeRoot` operation.

    You can use the `aws:SourceIdentity` condition key to control access
    based on the value of source identity. For more information about using
    source identity, see [Monitor and control actions taken with assumed
    roles](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_control-access_monitor.html)
    in the *IAM User Guide*.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: =,.@-
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ASSUME_ROOT_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.credentials is not None:
            serializer.write_struct(
                _SCHEMA_ASSUME_ROOT_OUTPUT.members["Credentials"], self.credentials
            )

        if self.source_identity is not None:
            serializer.write_string(
                _SCHEMA_ASSUME_ROOT_OUTPUT.members["SourceIdentity"],
                self.source_identity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["credentials"] = Credentials.deserialize(de)

                case 1:
                    kwargs["source_identity"] = de.read_string(
                        _SCHEMA_ASSUME_ROOT_OUTPUT.members["SourceIdentity"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ASSUME_ROOT_OUTPUT, consumer=_consumer)
        return kwargs


ASSUME_ROOT = APIOperation(
    input=AssumeRootInput,
    output=AssumeRootOutput,
    schema=_SCHEMA_ASSUME_ROOT,
    input_schema=_SCHEMA_ASSUME_ROOT_INPUT,
    output_schema=_SCHEMA_ASSUME_ROOT_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sts#ExpiredTokenException"): ExpiredTokenException,
            ShapeID(
                "com.amazonaws.sts#RegionDisabledException"
            ): RegionDisabledException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[_SCHEMA_EXPIRED_TOKEN_EXCEPTION, _SCHEMA_REGION_DISABLED_EXCEPTION],
)


@dataclass(kw_only=True)
class DecodeAuthorizationMessageInput:
    """Dataclass for DecodeAuthorizationMessageInput structure."""

    encoded_message: str | None = None
    """The encoded message that was returned with the response."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DECODE_AUTHORIZATION_MESSAGE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.encoded_message is not None:
            serializer.write_string(
                _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_INPUT.members["EncodedMessage"],
                self.encoded_message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["encoded_message"] = de.read_string(
                        _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_INPUT.members[
                            "EncodedMessage"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DecodeAuthorizationMessageOutput:
    """
    A document that contains additional information about the authorization
    status of a request from an encoded message that is returned in response
    to an Amazon Web Services request.
    """

    decoded_message: str | None = None
    """The API returns a response with the decoded message."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DECODE_AUTHORIZATION_MESSAGE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.decoded_message is not None:
            serializer.write_string(
                _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_OUTPUT.members["DecodedMessage"],
                self.decoded_message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["decoded_message"] = de.read_string(
                        _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_OUTPUT.members[
                            "DecodedMessage"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DECODE_AUTHORIZATION_MESSAGE_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class InvalidAuthorizationMessageException(ServiceError):
    """
    The error returned if the message passed to `DecodeAuthorizationMessage`
    was invalid. This can happen if the token contains invalid characters,
    such as line breaks, or if the message has expired.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_AUTHORIZATION_MESSAGE_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_AUTHORIZATION_MESSAGE_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_AUTHORIZATION_MESSAGE_EXCEPTION.members[
                            "message"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_INVALID_AUTHORIZATION_MESSAGE_EXCEPTION, consumer=_consumer
        )
        return kwargs


DECODE_AUTHORIZATION_MESSAGE = APIOperation(
    input=DecodeAuthorizationMessageInput,
    output=DecodeAuthorizationMessageOutput,
    schema=_SCHEMA_DECODE_AUTHORIZATION_MESSAGE,
    input_schema=_SCHEMA_DECODE_AUTHORIZATION_MESSAGE_INPUT,
    output_schema=_SCHEMA_DECODE_AUTHORIZATION_MESSAGE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.sts#InvalidAuthorizationMessageException"
            ): InvalidAuthorizationMessageException
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[_SCHEMA_INVALID_AUTHORIZATION_MESSAGE_EXCEPTION],
)


@dataclass(kw_only=True)
class GetAccessKeyInfoInput:
    """Dataclass for GetAccessKeyInfoInput structure."""

    access_key_id: str | None = None
    """
    The identifier of an access key.

    This parameter allows (through its regex pattern) a string of characters
    that can consist of any upper- or lowercase letter or digit.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_ACCESS_KEY_INFO_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.access_key_id is not None:
            serializer.write_string(
                _SCHEMA_GET_ACCESS_KEY_INFO_INPUT.members["AccessKeyId"],
                self.access_key_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["access_key_id"] = de.read_string(
                        _SCHEMA_GET_ACCESS_KEY_INFO_INPUT.members["AccessKeyId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_ACCESS_KEY_INFO_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetAccessKeyInfoOutput:
    """Dataclass for GetAccessKeyInfoOutput structure."""

    account: str | None = None
    """The number used to identify the Amazon Web Services account."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_ACCESS_KEY_INFO_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.account is not None:
            serializer.write_string(
                _SCHEMA_GET_ACCESS_KEY_INFO_OUTPUT.members["Account"], self.account
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["account"] = de.read_string(
                        _SCHEMA_GET_ACCESS_KEY_INFO_OUTPUT.members["Account"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_ACCESS_KEY_INFO_OUTPUT, consumer=_consumer)
        return kwargs


GET_ACCESS_KEY_INFO = APIOperation(
    input=GetAccessKeyInfoInput,
    output=GetAccessKeyInfoOutput,
    schema=_SCHEMA_GET_ACCESS_KEY_INFO,
    input_schema=_SCHEMA_GET_ACCESS_KEY_INFO_INPUT,
    output_schema=_SCHEMA_GET_ACCESS_KEY_INFO_OUTPUT,
    error_registry=TypeRegistry({}),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[],
)


@dataclass(kw_only=True)
class GetCallerIdentityInput:
    """Dataclass for GetCallerIdentityInput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_CALLER_IDENTITY_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_CALLER_IDENTITY_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetCallerIdentityOutput:
    """
    Contains the response to a successful GetCallerIdentity request,
    including information about the entity making the request.
    """

    user_id: str | None = None
    """
    The unique identifier of the calling entity. The exact value depends on
    the type of entity that is making the call. The values returned are
    those listed in the **aws:userid** column in the [Principal
    table](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_variables.html#principaltable)
    found on the **Policy Variables** reference page in the *IAM User
    Guide*.
    """

    account: str | None = None
    """
    The Amazon Web Services account ID number of the account that owns or
    contains the calling entity.
    """

    arn: str | None = None
    """The Amazon Web Services ARN associated with the calling entity."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_CALLER_IDENTITY_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.user_id is not None:
            serializer.write_string(
                _SCHEMA_GET_CALLER_IDENTITY_OUTPUT.members["UserId"], self.user_id
            )

        if self.account is not None:
            serializer.write_string(
                _SCHEMA_GET_CALLER_IDENTITY_OUTPUT.members["Account"], self.account
            )

        if self.arn is not None:
            serializer.write_string(
                _SCHEMA_GET_CALLER_IDENTITY_OUTPUT.members["Arn"], self.arn
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["user_id"] = de.read_string(
                        _SCHEMA_GET_CALLER_IDENTITY_OUTPUT.members["UserId"]
                    )

                case 1:
                    kwargs["account"] = de.read_string(
                        _SCHEMA_GET_CALLER_IDENTITY_OUTPUT.members["Account"]
                    )

                case 2:
                    kwargs["arn"] = de.read_string(
                        _SCHEMA_GET_CALLER_IDENTITY_OUTPUT.members["Arn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_CALLER_IDENTITY_OUTPUT, consumer=_consumer)
        return kwargs


GET_CALLER_IDENTITY = APIOperation(
    input=GetCallerIdentityInput,
    output=GetCallerIdentityOutput,
    schema=_SCHEMA_GET_CALLER_IDENTITY,
    input_schema=_SCHEMA_GET_CALLER_IDENTITY_INPUT,
    output_schema=_SCHEMA_GET_CALLER_IDENTITY_OUTPUT,
    error_registry=TypeRegistry({}),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[],
)


@dataclass(kw_only=True)
class ExpiredTradeInTokenException(ServiceError):
    """
    The trade-in token provided in the request has expired and can no longer
    be exchanged for credentials. Request a new token and retry the
    operation.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPIRED_TRADE_IN_TOKEN_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_EXPIRED_TRADE_IN_TOKEN_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_EXPIRED_TRADE_IN_TOKEN_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_EXPIRED_TRADE_IN_TOKEN_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class GetDelegatedAccessTokenInput:
    """Dataclass for GetDelegatedAccessTokenInput structure."""

    trade_in_token: str | None = field(repr=False, default=None)
    """
    The token to exchange for temporary Amazon Web Services credentials.
    This token must be valid and unexpired at the time of the request.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_DELEGATED_ACCESS_TOKEN_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.trade_in_token is not None:
            serializer.write_string(
                _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_INPUT.members["TradeInToken"],
                self.trade_in_token,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["trade_in_token"] = de.read_string(
                        _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_INPUT.members["TradeInToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class GetDelegatedAccessTokenOutput:
    """Dataclass for GetDelegatedAccessTokenOutput structure."""

    credentials: Credentials | None = None
    """Amazon Web Services credentials for API authentication."""

    packed_policy_size: int | None = None
    """
    The percentage of the maximum policy size that is used by the session
    policy. The policy size is calculated as the sum of all the session
    policies and permission boundaries attached to the session. If the
    packed size exceeds 100%, the request fails.
    """

    assumed_principal: str | None = None
    """
    The Amazon Resource Name (ARN) of the principal that was assumed when
    obtaining the delegated access token. This ARN identifies the IAM entity
    whose permissions are granted by the temporary credentials.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.credentials is not None:
            serializer.write_struct(
                _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT.members["Credentials"],
                self.credentials,
            )

        if self.packed_policy_size is not None:
            serializer.write_integer(
                _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT.members["PackedPolicySize"],
                self.packed_policy_size,
            )

        if self.assumed_principal is not None:
            serializer.write_string(
                _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT.members["AssumedPrincipal"],
                self.assumed_principal,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["credentials"] = Credentials.deserialize(de)

                case 1:
                    kwargs["packed_policy_size"] = de.read_integer(
                        _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT.members[
                            "PackedPolicySize"
                        ]
                    )

                case 2:
                    kwargs["assumed_principal"] = de.read_string(
                        _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT.members[
                            "AssumedPrincipal"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT, consumer=_consumer
        )
        return kwargs


GET_DELEGATED_ACCESS_TOKEN = APIOperation(
    input=GetDelegatedAccessTokenInput,
    output=GetDelegatedAccessTokenOutput,
    schema=_SCHEMA_GET_DELEGATED_ACCESS_TOKEN,
    input_schema=_SCHEMA_GET_DELEGATED_ACCESS_TOKEN_INPUT,
    output_schema=_SCHEMA_GET_DELEGATED_ACCESS_TOKEN_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.sts#ExpiredTradeInTokenException"
            ): ExpiredTradeInTokenException,
            ShapeID(
                "com.amazonaws.sts#PackedPolicyTooLargeException"
            ): PackedPolicyTooLargeException,
            ShapeID(
                "com.amazonaws.sts#RegionDisabledException"
            ): RegionDisabledException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[
        _SCHEMA_EXPIRED_TRADE_IN_TOKEN_EXCEPTION,
        _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION,
        _SCHEMA_REGION_DISABLED_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class GetFederationTokenInput:
    """Dataclass for GetFederationTokenInput structure."""

    name: str | None = None
    """
    The name of the federated user. The name is used as an identifier for
    the temporary security credentials (such as `Bob`). For example, you can
    reference the federated user name in a resource-based policy, such as in
    an Amazon S3 bucket policy.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: =,.@-
    """

    policy: str | None = None
    """
    An IAM policy in JSON format that you want to use as an inline session
    policy.

    You must pass an inline or managed [session
    policy](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    to this operation. You can pass a single JSON policy document to use as
    an inline session policy. You can also specify up to 10 managed policy
    Amazon Resource Names (ARNs) to use as managed session policies.

    This parameter is optional. However, if you do not pass any session
    policies, then the resulting federated user session has no permissions.

    When you pass session policies, the session permissions are the
    intersection of the IAM user policies and the session policies that you
    pass. This gives you a way to further restrict the permissions for a
    federated user. You cannot use session policies to grant more
    permissions than those that are defined in the permissions policy of the
    IAM user. For more information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.

    The resulting credentials can be used to access a resource that has a
    resource-based policy. If that policy specifically references the
    federated user session in the `Principal` element of the policy, the
    session has the permissions allowed by the policy. These permissions are
    granted in addition to the permissions that are granted by the session
    policies.

    The plaintext that you use for both inline and managed session policies
    can't exceed 2,048 characters. The JSON policy characters can be any
    ASCII character from the space character to the end of the valid
    character list (\\u0020 through \\u00FF). It can also include the tab
    (\\u0009), linefeed (\\u000A), and carriage return (\\u000D) characters.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.
    """

    policy_arns: list[PolicyDescriptorType] | None = None
    """
    The Amazon Resource Names (ARNs) of the IAM managed policies that you
    want to use as a managed session policy. The policies must exist in the
    same account as the IAM user that is requesting federated access.

    You must pass an inline or managed [session
    policy](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    to this operation. You can pass a single JSON policy document to use as
    an inline session policy. You can also specify up to 10 managed policy
    Amazon Resource Names (ARNs) to use as managed session policies. The
    plaintext that you use for both inline and managed session policies
    can't exceed 2,048 characters. You can provide up to 10 managed policy
    ARNs. For more information about ARNs, see [Amazon Resource Names (ARNs)
    and Amazon Web Services Service
    Namespaces](https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
    in the Amazon Web Services General Reference.

    This parameter is optional. However, if you do not pass any session
    policies, then the resulting federated user session has no permissions.

    When you pass session policies, the session permissions are the
    intersection of the IAM user policies and the session policies that you
    pass. This gives you a way to further restrict the permissions for a
    federated user. You cannot use session policies to grant more
    permissions than those that are defined in the permissions policy of the
    IAM user. For more information, see [Session
    Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#policies_session)
    in the *IAM User Guide*.

    The resulting credentials can be used to access a resource that has a
    resource-based policy. If that policy specifically references the
    federated user session in the `Principal` element of the policy, the
    session has the permissions allowed by the policy. These permissions are
    granted in addition to the permissions that are granted by the session
    policies.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.
    """

    duration_seconds: int | None = None
    """
    The duration, in seconds, that the session should last. Acceptable
    durations for federation sessions range from 900 seconds (15 minutes) to
    129,600 seconds (36 hours), with 43,200 seconds (12 hours) as the
    default. Sessions obtained using root user credentials are restricted to
    a maximum of 3,600 seconds (one hour). If the specified duration is
    longer than one hour, the session obtained by using root user
    credentials defaults to one hour.
    """

    tags: list[Tag] | None = None
    """
    A list of session tags. Each session tag consists of a key name and an
    associated value. For more information about session tags, see [Passing
    Session Tags in
    STS](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_session-tags.html)
    in the *IAM User Guide*.

    This parameter is optional. You can pass up to 50 session tags. The
    plaintext session tag keys can't exceed 128 characters and the values
    can't exceed 256 characters. For these and additional limits, see [IAM
    and STS Character
    Limits](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-limits.html#reference_iam-limits-entity-length)
    in the *IAM User Guide*.

    Note:
        An Amazon Web Services conversion compresses the passed inline session
        policy, managed policy ARNs, and session tags into a packed binary
        format that has a separate limit. Your request can fail for this limit
        even if your plaintext meets the other requirements. The
        `PackedPolicySize` response element indicates by percentage how close
        the policies and tags for your request are to the upper size limit.

    You can pass a session tag with the same key as a tag that is already
    attached to the user you are federating. When you do, session tags
    override a user tag with the same key.

    Tag key--value pairs are not case sensitive, but case is preserved. This
    means that you cannot have separate `Department` and `department` tag
    keys. Assume that the role has the `Department`=`Marketing` tag and you
    pass the `department`=`engineering` session tag. `Department` and
    `department` are not saved as separate tags, and the session tag passed
    in the request takes precedence over the role tag.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_FEDERATION_TOKEN_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.name is not None:
            serializer.write_string(
                _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["Name"], self.name
            )

        if self.policy is not None:
            serializer.write_string(
                _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["Policy"], self.policy
            )

        if self.policy_arns is not None:
            _serialize_policy_descriptor_list_type(
                serializer,
                _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["PolicyArns"],
                self.policy_arns,
            )

        if self.duration_seconds is not None:
            serializer.write_integer(
                _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["DurationSeconds"],
                self.duration_seconds,
            )

        if self.tags is not None:
            _serialize_tag_list_type(
                serializer,
                _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["Tags"],
                self.tags,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["name"] = de.read_string(
                        _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["Name"]
                    )

                case 1:
                    kwargs["policy"] = de.read_string(
                        _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["Policy"]
                    )

                case 2:
                    kwargs["policy_arns"] = _deserialize_policy_descriptor_list_type(
                        de, _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["PolicyArns"]
                    )

                case 3:
                    kwargs["duration_seconds"] = de.read_integer(
                        _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["DurationSeconds"]
                    )

                case 4:
                    kwargs["tags"] = _deserialize_tag_list_type(
                        de, _SCHEMA_GET_FEDERATION_TOKEN_INPUT.members["Tags"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_FEDERATION_TOKEN_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class FederatedUser:
    """
    Identifiers for the federated user that is associated with the
    credentials.
    """

    federated_user_id: str
    """
    The string that identifies the federated user associated with the
    credentials, similar to the unique ID of an IAM user.
    """

    arn: str
    """
    The ARN that specifies the federated user that is associated with the
    credentials. For more information about ARNs and how to use them in
    policies, see [IAM
    Identifiers](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html)
    in the *IAM User Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_FEDERATED_USER, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_FEDERATED_USER.members["FederatedUserId"], self.federated_user_id
        )
        serializer.write_string(_SCHEMA_FEDERATED_USER.members["Arn"], self.arn)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["federated_user_id"] = de.read_string(
                        _SCHEMA_FEDERATED_USER.members["FederatedUserId"]
                    )

                case 1:
                    kwargs["arn"] = de.read_string(
                        _SCHEMA_FEDERATED_USER.members["Arn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_FEDERATED_USER, consumer=_consumer)
        if "federated_user_id" not in kwargs:
            kwargs["federated_user_id"] = ""
        if "arn" not in kwargs:
            kwargs["arn"] = ""
        return kwargs


@dataclass(kw_only=True)
class GetFederationTokenOutput:
    """
    Contains the response to a successful GetFederationToken request,
    including temporary Amazon Web Services credentials that can be used to
    make Amazon Web Services requests.
    """

    credentials: Credentials | None = None
    """
    The temporary security credentials, which include an access key ID, a
    secret access key, and a security (or session) token.

    Note:
        The size of the security token that STS API operations return is not
        fixed. We strongly recommend that you make no assumptions about the
        maximum size.
    """

    federated_user: FederatedUser | None = None
    """
    Identifiers for the federated user associated with the credentials (such
    as `arn:aws:sts::123456789012:federated-user/Bob` or
    `123456789012:Bob`). You can use the federated user's ARN in your
    resource-based policies, such as an Amazon S3 bucket policy.
    """

    packed_policy_size: int | None = None
    """
    A percentage value that indicates the packed size of the session
    policies and session tags combined passed in the request. The request
    fails if the packed size is greater than 100 percent, which means the
    policies and tags exceeded the allowed space.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_FEDERATION_TOKEN_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.credentials is not None:
            serializer.write_struct(
                _SCHEMA_GET_FEDERATION_TOKEN_OUTPUT.members["Credentials"],
                self.credentials,
            )

        if self.federated_user is not None:
            serializer.write_struct(
                _SCHEMA_GET_FEDERATION_TOKEN_OUTPUT.members["FederatedUser"],
                self.federated_user,
            )

        if self.packed_policy_size is not None:
            serializer.write_integer(
                _SCHEMA_GET_FEDERATION_TOKEN_OUTPUT.members["PackedPolicySize"],
                self.packed_policy_size,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["credentials"] = Credentials.deserialize(de)

                case 1:
                    kwargs["federated_user"] = FederatedUser.deserialize(de)

                case 2:
                    kwargs["packed_policy_size"] = de.read_integer(
                        _SCHEMA_GET_FEDERATION_TOKEN_OUTPUT.members["PackedPolicySize"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GET_FEDERATION_TOKEN_OUTPUT, consumer=_consumer
        )
        return kwargs


GET_FEDERATION_TOKEN = APIOperation(
    input=GetFederationTokenInput,
    output=GetFederationTokenOutput,
    schema=_SCHEMA_GET_FEDERATION_TOKEN,
    input_schema=_SCHEMA_GET_FEDERATION_TOKEN_INPUT,
    output_schema=_SCHEMA_GET_FEDERATION_TOKEN_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.sts#MalformedPolicyDocumentException"
            ): MalformedPolicyDocumentException,
            ShapeID(
                "com.amazonaws.sts#PackedPolicyTooLargeException"
            ): PackedPolicyTooLargeException,
            ShapeID(
                "com.amazonaws.sts#RegionDisabledException"
            ): RegionDisabledException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[
        _SCHEMA_MALFORMED_POLICY_DOCUMENT_EXCEPTION,
        _SCHEMA_PACKED_POLICY_TOO_LARGE_EXCEPTION,
        _SCHEMA_REGION_DISABLED_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class GetSessionTokenInput:
    """Dataclass for GetSessionTokenInput structure."""

    duration_seconds: int | None = None
    """
    The duration, in seconds, that the credentials should remain valid.
    Acceptable durations for IAM user sessions range from 900 seconds (15
    minutes) to 129,600 seconds (36 hours), with 43,200 seconds (12 hours)
    as the default. Sessions for Amazon Web Services account owners are
    restricted to a maximum of 3,600 seconds (one hour). If the duration is
    longer than one hour, the session for Amazon Web Services account owners
    defaults to one hour.
    """

    serial_number: str | None = None
    """
    The identification number of the MFA device that is associated with the
    IAM user who is making the `GetSessionToken` call. Specify this value if
    the IAM user has a policy that requires MFA authentication. The value is
    either the serial number for a hardware device (such as `GAHT12345678`)
    or an Amazon Resource Name (ARN) for a virtual device (such as
    `arn:aws:iam::123456789012:mfa/user`). You can find the device for an
    IAM user by going to the Amazon Web Services Management Console and
    viewing the user's security credentials.

    The regex used to validate this parameter is a string of characters
    consisting of upper- and lower-case alphanumeric characters with no
    spaces. You can also include underscores or any of the following
    characters: =,.@:/-
    """

    token_code: str | None = None
    """
    The value provided by the MFA device, if MFA is required. If any policy
    requires the IAM user to submit an MFA code, specify this value. If MFA
    authentication is required, the user must provide a code when requesting
    a set of temporary security credentials. A user who fails to provide the
    code receives an \"access denied\" response when requesting resources
    that require MFA authentication.

    The format for this parameter, as described by its regex pattern, is a
    sequence of six numeric digits.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_SESSION_TOKEN_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.duration_seconds is not None:
            serializer.write_integer(
                _SCHEMA_GET_SESSION_TOKEN_INPUT.members["DurationSeconds"],
                self.duration_seconds,
            )

        if self.serial_number is not None:
            serializer.write_string(
                _SCHEMA_GET_SESSION_TOKEN_INPUT.members["SerialNumber"],
                self.serial_number,
            )

        if self.token_code is not None:
            serializer.write_string(
                _SCHEMA_GET_SESSION_TOKEN_INPUT.members["TokenCode"], self.token_code
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["duration_seconds"] = de.read_integer(
                        _SCHEMA_GET_SESSION_TOKEN_INPUT.members["DurationSeconds"]
                    )

                case 1:
                    kwargs["serial_number"] = de.read_string(
                        _SCHEMA_GET_SESSION_TOKEN_INPUT.members["SerialNumber"]
                    )

                case 2:
                    kwargs["token_code"] = de.read_string(
                        _SCHEMA_GET_SESSION_TOKEN_INPUT.members["TokenCode"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_SESSION_TOKEN_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetSessionTokenOutput:
    """
    Contains the response to a successful GetSessionToken request, including
    temporary Amazon Web Services credentials that can be used to make
    Amazon Web Services requests.
    """

    credentials: Credentials | None = None
    """
    The temporary security credentials, which include an access key ID, a
    secret access key, and a security (or session) token.

    Note:
        The size of the security token that STS API operations return is not
        fixed. We strongly recommend that you make no assumptions about the
        maximum size.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_SESSION_TOKEN_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.credentials is not None:
            serializer.write_struct(
                _SCHEMA_GET_SESSION_TOKEN_OUTPUT.members["Credentials"],
                self.credentials,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["credentials"] = Credentials.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_SESSION_TOKEN_OUTPUT, consumer=_consumer)
        return kwargs


GET_SESSION_TOKEN = APIOperation(
    input=GetSessionTokenInput,
    output=GetSessionTokenOutput,
    schema=_SCHEMA_GET_SESSION_TOKEN,
    input_schema=_SCHEMA_GET_SESSION_TOKEN_INPUT,
    output_schema=_SCHEMA_GET_SESSION_TOKEN_OUTPUT,
    error_registry=TypeRegistry(
        {ShapeID("com.amazonaws.sts#RegionDisabledException"): RegionDisabledException}
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[_SCHEMA_REGION_DISABLED_EXCEPTION],
)


def _serialize_web_identity_token_audience_list_type(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_web_identity_token_audience_list_type(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class GetWebIdentityTokenInput:
    """Dataclass for GetWebIdentityTokenInput structure."""

    audience: list[str] | None = None
    """
    The intended recipient of the web identity token. This value populates
    the `aud` claim in the JWT and should identify the service or
    application that will validate and use the token. The external service
    should verify this claim to ensure the token was intended for their use.
    """

    duration_seconds: int | None = None
    """
    The duration, in seconds, for which the JSON Web Token (JWT) will remain
    valid. The value can range from 60 seconds (1 minute) to 3600 seconds (1
    hour). If not specified, the default duration is 300 seconds (5
    minutes). The token is designed to be short-lived and should be used for
    proof of identity, then exchanged for credentials or short-lived tokens
    in the external service.
    """

    signing_algorithm: str | None = None
    """
    The cryptographic algorithm to use for signing the JSON Web Token (JWT).
    Valid values are RS256 (RSA with SHA-256) and ES384 (ECDSA using P-384
    curve with SHA-384).
    """

    tags: list[Tag] | None = None
    """
    An optional list of tags to include in the JSON Web Token (JWT). These
    tags are added as custom claims to the JWT and can be used by the
    downstream service for authorization decisions.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.audience is not None:
            _serialize_web_identity_token_audience_list_type(
                serializer,
                _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["Audience"],
                self.audience,
            )

        if self.duration_seconds is not None:
            serializer.write_integer(
                _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["DurationSeconds"],
                self.duration_seconds,
            )

        if self.signing_algorithm is not None:
            serializer.write_string(
                _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["SigningAlgorithm"],
                self.signing_algorithm,
            )

        if self.tags is not None:
            _serialize_tag_list_type(
                serializer,
                _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["Tags"],
                self.tags,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["audience"] = (
                        _deserialize_web_identity_token_audience_list_type(
                            de, _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["Audience"]
                        )
                    )

                case 1:
                    kwargs["duration_seconds"] = de.read_integer(
                        _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["DurationSeconds"]
                    )

                case 2:
                    kwargs["signing_algorithm"] = de.read_string(
                        _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["SigningAlgorithm"]
                    )

                case 3:
                    kwargs["tags"] = _deserialize_tag_list_type(
                        de, _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT.members["Tags"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class GetWebIdentityTokenOutput:
    """Dataclass for GetWebIdentityTokenOutput structure."""

    web_identity_token: str | None = field(repr=False, default=None)
    """
    A signed JSON Web Token (JWT) that represents the caller's Amazon Web
    Services identity. The token contains standard JWT claims such as
    subject, audience, expiration time, and additional identity attributes
    added by STS as custom claims. You can also add your own custom claims
    to the token by passing tags as request parameters to the
    `GetWebIdentityToken` API. The token is signed using the specified
    signing algorithm and can be verified using the verification keys
    available at the issuer's JWKS endpoint.
    """

    expiration: datetime | None = None
    """
    The date and time when the web identity token expires, in UTC. The
    expiration is determined by adding the `DurationSeconds` value to the
    time the token was issued. After this time, the token should no longer
    be considered valid.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.web_identity_token is not None:
            serializer.write_string(
                _SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT.members["WebIdentityToken"],
                self.web_identity_token,
            )

        if self.expiration is not None:
            serializer.write_timestamp(
                _SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT.members["Expiration"],
                self.expiration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["web_identity_token"] = de.read_string(
                        _SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT.members[
                            "WebIdentityToken"
                        ]
                    )

                case 1:
                    kwargs["expiration"] = de.read_timestamp(
                        _SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT.members["Expiration"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class JWTPayloadSizeExceededException(ServiceError):
    """
    The requested token payload size exceeds the maximum allowed size.
    Reduce the number of request tags included in the `GetWebIdentityToken`
    API call to reduce the token payload size.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_JWT_PAYLOAD_SIZE_EXCEEDED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_JWT_PAYLOAD_SIZE_EXCEEDED_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_JWT_PAYLOAD_SIZE_EXCEEDED_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_JWT_PAYLOAD_SIZE_EXCEEDED_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class OutboundWebIdentityFederationDisabledException(ServiceError):
    """
    The outbound web identity federation feature is not enabled for this
    account. To use this feature, you must first enable it through the
    Amazon Web Services Management Console or API.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_OUTBOUND_WEB_IDENTITY_FEDERATION_DISABLED_EXCEPTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_OUTBOUND_WEB_IDENTITY_FEDERATION_DISABLED_EXCEPTION.members[
                    "message"
                ],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_OUTBOUND_WEB_IDENTITY_FEDERATION_DISABLED_EXCEPTION.members[
                            "message"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_OUTBOUND_WEB_IDENTITY_FEDERATION_DISABLED_EXCEPTION,
            consumer=_consumer,
        )
        return kwargs


@dataclass(kw_only=True)
class SessionDurationEscalationException(ServiceError):
    """
    The requested token duration would extend the session beyond its
    original expiration time. You cannot use this operation to extend the
    lifetime of a session beyond what was granted when the session was
    originally created.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SESSION_DURATION_ESCALATION_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_SESSION_DURATION_ESCALATION_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_SESSION_DURATION_ESCALATION_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_SESSION_DURATION_ESCALATION_EXCEPTION, consumer=_consumer
        )
        return kwargs


GET_WEB_IDENTITY_TOKEN = APIOperation(
    input=GetWebIdentityTokenInput,
    output=GetWebIdentityTokenOutput,
    schema=_SCHEMA_GET_WEB_IDENTITY_TOKEN,
    input_schema=_SCHEMA_GET_WEB_IDENTITY_TOKEN_INPUT,
    output_schema=_SCHEMA_GET_WEB_IDENTITY_TOKEN_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.sts#JWTPayloadSizeExceededException"
            ): JWTPayloadSizeExceededException,
            ShapeID(
                "com.amazonaws.sts#OutboundWebIdentityFederationDisabledException"
            ): OutboundWebIdentityFederationDisabledException,
            ShapeID(
                "com.amazonaws.sts#SessionDurationEscalationException"
            ): SessionDurationEscalationException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4"), ShapeID("aws.auth#sigv4a")],
    error_schemas=[
        _SCHEMA_JWT_PAYLOAD_SIZE_EXCEEDED_EXCEPTION,
        _SCHEMA_OUTBOUND_WEB_IDENTITY_FEDERATION_DISABLED_EXCEPTION,
        _SCHEMA_SESSION_DURATION_ESCALATION_EXCEPTION,
    ],
)
